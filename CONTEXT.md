# 个人设备配置

本仓库描述个人设备应具备的软件、系统行为与恢复边界，当前只覆盖 macOS，后续可以扩展其他平台

## 配置边界

**系统配置仓库**：
独立保存 Nix、nix-darwin、Home Manager 及软件清单的 Git 仓库，新设备通过它构建系统配置
_避免使用_：flake dotfiles

**Dotfiles 仓库**：
由 chezmoi 维护的用户配置仓库，不再包含系统配置仓库
_避免使用_：主配置仓库

**恢复**：
让新设备达到已声明状态的操作，只安装缺失内容，不主动刷新滚动版本或上游配置
_避免使用_：更新、升级

**维护更新**：
由用户显式触发并审阅的软件版本、Flake inputs 或上游配置刷新
_避免使用_：恢复、自动更新

**人工恢复步骤**：
无法或不适合声明式执行的首次安装、登录、授权和官网应用操作，以简短文档记录
_避免使用_：自动化缺口

## 配置所有权

**唯一所有者**：
对一个目标文件或安装对象拥有最终写入权的工具，同一对象不得由多个工具同时声明
_避免使用_：主要管理器

**Rime 上游文件**：
`~/Library/Rime` Git 仓库中由 Oh My Rime 跟踪的文件，只允许通过快进拉取更新

**Rime 自定义文件**：
`~/Library/Rime` 中未被 Oh My Rime 跟踪、由 chezmoi 明确管理的 custom 文件

**滚动开发工具**：
macOS 上由 mise 语义选择器解析和安装的开发运行时或 CLI，不承诺跨时间得到完全相同版本
_避免使用_：锁定工具

**主机配置名**：
Flake 中用于选择一组设备配置的非识别性逻辑名称，不等同于设备真实主机名，也不负责修改系统主机名
_避免使用_：主机名、ComputerName

**人工安装软件**：
通过官网安装包安装并由厂商更新器维护的软件，只记录恢复步骤，不纳入 Homebrew 清单
_避免使用_：未声明软件

**Chezmoi 渲染密钥**：
由 chezmoi 在应用配置或 shell 配置中从 Bitwarden 取值并写入本机目标文件的密钥，不允许进入系统配置仓库或 Nix store
_避免使用_：Nix 密钥

**上游配置仓库**：
Rime 与 tmux 等由第三方维护、在恢复时 clone 并只通过显式维护命令快进更新的 Git 仓库
_避免使用_：Nix 模块、自动更新仓库
